# with
# 		comment #wew
role="browser"
	wtf "$WID" && waitron group_add_window 2

# match all
name=".*"
	echo "created window with id $RULER_WID"

# only works with ignore case (-i)
class="urxvt"
	echo "hello urxvt"
